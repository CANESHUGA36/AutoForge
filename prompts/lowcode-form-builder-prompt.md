# Low-Code Form Builder Prompt

```bash
python run.py "Build a low-code visual form builder. Left sidebar has draggable form components (text input, select, checkbox, radio, date picker, switch, textarea). Center canvas drops components in vertical stack, click to select, drag to reorder, click to delete. Right panel edits properties: label, placeholder, required toggle, options. Top toolbar toggles preview mode, exports/imports JSON schema. Preview mode renders functional form with validation. Tech: Vite+React+TypeScript+Tailwind. Use CSS display for all visibility, NEVER {condition && <Element />}."
```
